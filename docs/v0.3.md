# DDT 弹弹堂 — 项目文档 v0.3

> 版本状态：开发完成，待推送

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 技术选型](#2-技术选型)
- [3. 项目架构](#3-项目架构)
- [4. 通信协议](#4-通信协议)
- [5. 服务端](#5-服务端)
- [6. 客户端](#6-客户端)
- [7. RPC 框架](#7-rpc-框架)
- [8. 构建与部署](#8-构建与部署)
- [9. 测试指南](#9-测试指南)
- [10. 实现细节](#10-实现细节)
- [11. 版本更新日志（相对 v0.2）](#11-版本更新日志相对-v02)

---

## 1. 项目概述

DDT（弹弹堂）是一款基于 C/S 架构的实时多人回合制弹道射击游戏。玩家在同一张地形图上轮流发射炮弹，考虑风向和角度，击中对手以减少其 HP，率先将对方 HP 降为零者获胜。

项目基于 sylar C++ 协程框架构建服务端，客户端使用 OpenGL 3.3 渲染，通过 WebSocket 进行实时通信。整体分为服务端（gate/lobby/match/battle 四层）和客户端（逻辑层/渲染层/网络层）两大模块，附带一套基于 Protobuf + ZooKeeper 的 RPC 框架。

### 核心功能

- **回合制弹道射击**：服务端权威物理引擎，客户端插值播放
- **房间系统**：创建/加入/离开/准备/切换队伍，对局结束后返回房间直接开下一局
- **聊天系统**：Team/All/Room/World/System/Broadcast/Private 七频道
- **账号系统**：注册/登录，MySQL 存储 + Redis 会话管理
- **Protobuf 通信协议**：40+ 消息类型，`oneof payload` 多态，支持 import 子 proto
- **RPC 框架**：Protobuf + ZooKeeper 服务注册与发现
- **自动重连**：断线后指数退避自动重连，重连后自动登录恢复状态

---

## 2. 技术选型

### 2.1 服务端

| 组件 | 技术 | 用途 |
|------|------|------|
| 框架 | sylar | C++ 协程框架，提供 IOManager、TcpServer、WebSocket、日志、配置等基础设施 |
| 序列化 | Protobuf | 游戏协议定义与序列化 |
| 通信 | WebSocket | 双向实时通信 |
| 数据库 | MySQL | 账号、玩家资料、聊天记录、对战记录、好友关系持久化 |
| 缓存 | Redis | 会话 token、在线状态、房间缓存 |
| 配置 | YAML | 游戏参数配置（物理、地形、战斗） |
| 服务发现 | ZooKeeper | RPC 服务注册与发现 |
| 构建 | CMake + Make | 编译构建 |

### 2.2 客户端

| 组件 | 技术 | 用途 |
|------|------|------|
| 窗口/输入 | GLFW | 跨平台窗口管理与输入事件 |
| 渲染 | OpenGL 3.3 Core | 2D 精灵渲染、地形渲染、粒子效果 |
| 文字 | FreeType | 中文文字渲染 |
| 贴图 | stb_image | PNG 纹理加载 |
| 数学 | GLM | 矩阵变换、正交投影 |
| UI | ImGui | 游戏界面面板（登录、房间、聊天等） |
| 网络 | 原生 Socket | WebSocket 客户端连接 |
| UI 集成 | ImGui + GLFW + OpenGL3 | ImGui 后端绑定 |

---

## 3. 项目架构

### 3.1 目录结构

```
sylar/
├── sylar/                       # sylar 框架源码
│   ├── log.h/.cc                # 日志系统
│   ├── fiber.h/.cc              # 协程
│   ├── iomanager.h/.cc          # IO 调度器
│   ├── tcp_server.h/.cc         # TCP 服务器
│   ├── http/ws_server.h/.cc     # WebSocket 服务器
│   ├── config.h/.cc             # 配置系统
│   └── rpc/                     # RPC 框架扩展
├── common/                      # [NEW] 客户端/服务端共享代码
│   └── ddt_physics.h/.cc        # 物理引擎（弹道模拟、坡度计算）
├── ddt_server/                  # DDT 游戏服务端
│   ├── gate/                    # 网关层
│   │   ├── ddt_main.cc          # 服务端入口
│   │   ├── ddt_servlet.h/.cc    # 消息路由（已拆分为独立 handler）
│   │   └── ddt_session_mgr.h/.cc # [NEW] Session 生命周期管理
│   ├── lobby/                   # 大厅层（认证 + 聊天）
│   ├── match/                   # 匹配层（房间 + 玩家管理）
│   ├── battle/                  # 战斗层
│   ├── common/                  # 共享配置
│   ├── db/                      # 数据库操作
│   ├── conf/                    # 配置文件
│   └── schema.sql               # 数据库建表脚本
├── ddt_client/                  # DDT 游戏客户端
│   ├── logic/                   # 游戏逻辑
│   │   ├── game.h/.cc           # [REFACTORED] 顶层协调者（630行）
│   │   ├── game_hud.h/.cc       # [NEW] ImGui 面板（558行）
│   │   ├── game_network.h/.cc   # [NEW] 网络消息处理（437行）
│   │   ├── game_renderer.h/.cc  # [NEW] 世界渲染（205行）
│   │   ├── game_object.h/.cc    # 游戏对象基类
│   │   └── projectile.h/.cc     # 弹道物体
│   ├── render/                  # 渲染引擎
│   ├── network/                 # 网络通信
│   ├── battle/                  # 战斗相关
│   ├── ui/                      # UI 界面
│   ├── shaders/                 # GLSL 着色器
│   └── assets/                  # 纹理贴图
├── proto/                       # Protobuf 协议定义
│   ├── ddt.proto                # 主协议（import 子 proto）
│   ├── common.proto             # 共享类型
│   └── room.proto               # 房间相关消息
├── rpc_test/                    # RPC 测试代码
├── scripts/                     # 构建/发布脚本
├── docs/                        # 版本文档
│   ├── v0.1.md                  # 初始版本文档
│   ├── v0.2.md                  # Bug Fix 文档
│   └── v0.3.md                  # 本文档
└── CMakeLists.txt               # 根构建文件
```

### 3.2 系统架构图

```
┌─────────────────────────── Client ──────────────────────────────┐
│  main.cc (GLFW+ImGui 主循环, fixed timestep)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────┐     │
│  │ game.cc  │ │game_     │ │game_     │ │ render/       │     │
│  │ (协调者) │ │network.cc│ │renderer  │ │ terrain/sprite│     │
│  │  630行   │ │ 437行    │ │ 205行    │ │ batch/camera  │     │
│  └────┬─────┘ └────┬─────┘ └──────────┘ └───────────────┘     │
│       │             │                                           │
│  ┌────┴─────┐       │                                           │
│  │game_hud  │       │                                           │
│  │ 558行    │       │                                           │
│  └──────────┘       │                                           │
└───────────┼─────────┼───────────────────────────────────────────┘
            │ Protobuf│ WebSocket (shared_ptr<WSConnection>)
            │         ▼
┌───────────┼──── Server ─────────────────────────────────────────┐
│           │  ┌──────────────────────────────────────┐           │
│           │  │ gate/ddt_servlet.cc                  │           │
│           │  │ (独立 handler 方法 + SessionManager)  │           │
│           │  └───┬──────────┬───────────────┬──────┘           │
│           │      │          │               │                   │
│  ┌────────┴──┐ ┌─┴──────┐ ┌┴──────────┐ ┌──┴──────────┐      │
│  │lobby/     │ │match/  │ │battle/    │ │gate/        │      │
│  │auth+chat  │ │room_mgr│ │physics    │ │session_mgr  │      │
│  └───────────┘ │game_   │ │(→common/) │ │(NEW)        │      │
│                │room.cc │ └───────────┘ └─────────────┘      │
│                └───┬────┘                                     │
│  ┌─────────────────┴────┐ ┌──────────────┐                    │
│  │ db/                  │ │common/       │                    │
│  │ mysql + redis pool   │ │config        │                    │
│  └──────────────────────┘ └──────────────┘                    │
└────────────────────────────────────────────────────────────────┘
```

---

## 4. 通信协议

### 4.1 Protobuf 定义

协议定义在 `proto/ddt.proto`，通过 `import` 引入子 proto 文件，使用 `oneof payload` 实现多态消息：

```protobuf
message GameMessage {
  oneof payload {
    LoginRequest       login_request       = 1;
    LoginResponse      login_response      = 2;
    JoinRoomRequest    join_room_request   = 3;
    // ... 40+ 消息类型
  }
}
```

### 4.2 消息分类

| 类别 | 消息 | 说明 |
|------|------|------|
| **账号** | Login / Register | 登录注册 |
| **房间** | JoinRoom / CreateRoom / RoomList / LeaveRoom / Ready / SwitchTeam | 房间管理 |
| **战斗** | TurnStart / Shoot / ShootResult / Move / GameOver | 回合制战斗 |
| **聊天** | Chat / ChatHistory / PrivateChat | 多频道聊天 |
| **好友** | FriendAdd / FriendList | 好友系统 |
| **通知** | Error / ServerShutdown / OpponentLeft | 系统通知 |

### 4.3 战斗数据流

```
TurnStartNotify (服务端 → 双方)
  ├── turn_player_id: 当前回合玩家
  ├── wind: 风力值
  ├── turn_number: 回合数
  └── [NEW] 双方完整位置/HP/角度/方向（回合状态校准）

ShootRequest (当前玩家 → 服务端)
  ├── angle: 发射角度（绝对角度，含坡度补偿）
  └── force: 发射力度

ShootResultNotify (服务端 → 双方)
  ├── shooter_id: 射手 ID
  ├── points[]: 弹道轨迹点 (x, y, t)
  ├── hit_x, hit_y: 落点坐标
  ├── hit_player: 是否命中玩家
  ├── damage: 伤害值
  ├── damage_type: NORMAL / CRITICAL / BLOCK
  ├── direction: [NEW] 发射方向（客户端本地弹道方向转换）
  └── updated_player1/2: 更新后的玩家状态

RoomReadyNotify (服务端 → 双方)
  ├── ... 房间信息
  ├── physics_params: [NEW] 服务端物理参数下发
  └── max_move_per_turn: [NEW] 移动限额配置下发
```

---

## 5. 服务端

### 5.1 分层架构

#### Gate 层（`ddt_server/gate/`）

- `ddt_main.cc`：服务端入口，初始化 IOManager、数据库、房间管理器，启动 WebSocket 服务器
- `ddt_servlet.h/.cc`：消息路由，已拆分为独立 `handle*()` 方法（按消息类型分发），消除 200 行 switch-case
- **`ddt_session_mgr.h/.cc` [NEW]**：Session 生命周期管理，封装 session→player 映射、账号踢人、心跳清理

#### Lobby 层（`ddt_server/lobby/`）

- `ddt_auth.h/.cc`：账号认证，登录/注册处理，密码加盐哈希，token 生成与验证
- `ddt_chat_manager.h/.cc`：聊天管理，支持 Team/All/Room/World/System/Broadcast/Private 七个频道，聊天记录持久化

#### Match 层（`ddt_server/match/`）

- `ddt_room_manager.h/.cc`：房间管理，创建/销毁/列表/匹配，`leaveRoom()` 通过 Actor 异步执行确保线程安全
- `ddt_game_room.h/.cc`：单个房间逻辑，Actor 模式串行化房间状态修改，回合状态校准
- `ddt_player.h/.cc`：玩家数据，HP/位置/角度/方向/状态

#### Battle 层 → `common/`（`ddt_server/battle/`）

- `ddt_physics.h/.cc`：物理引擎移至 `common/` 目录共享，弹道模拟（重力+风力+空气阻力），地形碰撞检测，伤害计算，坡度角度计算

#### Common 层（`ddt_server/common/`）

- `ddt_config.h/.cc`：配置加载，从 `conf/game.yml` 读取物理参数、游戏规则、服务器配置

#### DB 层（`ddt_server/db/`）

- `ddt_database.h/.cc`：MySQL 连接池，CRUD 操作封装

### 5.2 物理引擎

弹道模拟参数（`conf/game.yml`），服务端通过 `RoomReadyNotify` 下发给客户端：

```yaml
physics:
  air_factor: 0.89927083
  wind_factor: 5.8709153
  gravity_factor: -172.06527992
  force_factor: 41.0
  dt: 0.01
  max_move_per_turn: 200
```

弹道方程（每帧）：
```
vx *= air_factor
vx += wind * wind_factor * dt
vy += gravity * dt
x  += vx * dt
y  += vy * dt
```

### 5.3 数据库

`schema.sql` 定义 5 张表：

| 表 | 用途 |
|----|------|
| `accounts` | 账号（用户名、密码哈希、盐） |
| `player_profiles` | 玩家资料（昵称、等级、经验、胜败） |
| `chat_history` | 聊天记录（频道、发送者、内容、时间） |
| `game_records` | 对战记录 |
| `friends` | 好友关系 |

---

## 6. 客户端

### 6.1 模块划分

#### 逻辑层（`ddt_client/logic/`）

| 文件 | 职责 |
|------|------|
| `game.h/.cc` | 顶层协调者（Init、ProcessInput、Update、updateCamera、Shutdown）— **630 行** |
| **`game_hud.h/.cc` [NEW]** | ImGui 面板渲染（HUD、房间列表、聊天、菜单、蓄力交互）— **558 行** |
| **`game_network.h/.cc` [NEW]** | 网络消息解析与游戏状态更新（processNetworkMessages）— **437 行** |
| **`game_renderer.h/.cc` [NEW]** | 世界渲染（地形、人物、弹道、粒子、爆炸、伤害数字、小地图）— **205 行** |
| `game_object.h/.cc` | 游戏对象基类（位置、大小、旋转、颜色、纹理） |
| `projectile.h/.cc` | 弹道物体：根据服务端轨迹点插值渲染飞行弹丸 |

#### 渲染层（`ddt_client/render/`）

| 文件 | 职责 |
|------|------|
| `shader.h/.cc` | GLSL 着色器编译、链接、uniform 设置 |
| `texture.h/.cc` | OpenGL 纹理封装：生成、绑定、格式设置 |
| `resource_manager.h/.cc` | 全局资源管理：Shader/Texture 按名称缓存，路径自动检测 |
| `sprite_renderer.h/.cc` | 单精灵渲染器 |
| `sprite_batch.h/.cc` | 批量精灵渲染：按纹理排序合并 draw call，6 顶点/精灵 |
| `terrain.h/.cc` | 地形渲染：高度图生成、地形线段绘制、爆炸破坏，**新增 `Reset()` 方法支持新局地形重建** |
| `text_renderer.h/.cc` | FreeType 文字渲染：加载字体、渲染中文文本 |
| `camera.h/.cc` | 摄像机系统：跟随玩家/弹道，右键拖拽平移 |

#### 网络层（`ddt_client/network/`）

| 文件 | 职责 |
|------|------|
| `ws_client.h/.cc` | WebSocket 客户端：连接、发送、接收 |
| `network_client.h/.cc` | 网络客户端：Protobuf 序列化/反序列化，消息回调分发，`shared_ptr` 安全连接管理，自动重连（指数退避） |

### 6.2 渲染流程

```
Begin(projection)
  ├── 绘制背景（bg_rainbow / bg_ghost）
  ├── 绘制地形（Terrain::Draw）
  ├── 绘制玩家精灵（SpriteBatch::Draw × 2，含坡度旋转）
  ├── 绘制弹道（Projectile::Draw）
  ├── 绘制爆炸效果
  ├── 绘制伤害浮字
  └── 绘制 UI（ImGui）
End()
  └── flush() — 按纹理排序，合并 draw call
```

### 6.3 摄像机模式

| 模式 | 行为 |
|------|------|
| INTRO | 开场镜头 |
| FOLLOW_TURN | 跟随当前回合玩家 |
| FOLLOW_PROJ | 跟随飞行中的弹丸 |
| MANUAL | 右键拖拽自由平移 |

### 6.4 坐标系统

使用 Y 轴向下的正交投影：

```cpp
glm::ortho(left, right, bottom, top, near, far)
// bottom > top → Y 轴向下
```

纹理加载不翻转（`stbi_set_flip_vertically_on_load` 设为 false），与 Y-down 投影配合直接映射。

### 6.5 纹理资源

共 36 张 PNG 纹理，存放在 `ddt_client/assets/`：

| 类别 | 文件 | 尺寸 |
|------|------|------|
| 角色 | player1.png, player1_r.png, player2.png, player2_r.png, role.png, role_r.png, role2.png, role2_r.png | 40×40 ~ 40×59 |
| 武器 | tri_darts, ice_cream 及其 _r/_bomb 变体, projectile 及其 _r/_bomb 变体 | 30×30 ~ 40×40 |
| 效果 | bow0-3, explosion0-3, bomb, fly, flyAttack, flyAttack_r | 40×40 ~ 80×80 |
| 背景 | bg_rainbow, bg_ghost | 1000×600 |
| 启动画面 | bg_start | 1000×657 |

### 6.6 着色器

`shaders/sprite.vert` / `shaders/sprite.frag`：OpenGL 3.3 Core Profile，顶点着色器完成 model × projection 变换，片段着色器进行纹理采样与颜色混合。

---

## 7. RPC 框架

在 sylar 框架基础上扩展了一套基于 Protobuf + ZooKeeper 的 RPC 框架，位于 `sylar/rpc/`。

### 7.1 架构

```
Caller (客户端)                          Callee (服务端)
    │                                        │
    │  RpcChannel.CallMethod()               │  RpcProvider
    │  ├── ZK 查找服务地址                    │  ├── notifyService()
    │  ├── 序列化请求                         │  ├── ZK 注册服务
    │  ├── Socket 连接                        │  └── handleClient()
    │  └── 发送/接收                          │      ├── 解析 RpcHeader
    │                                        │      ├── 查找 method
    │  <──── TCP ────>                        │      └── CallMethod()
```

### 7.2 线路协议

```
请求: [4字节 header_size (网络序)][RpcHeader protobuf][args protobuf]
响应: [4字节 response_size (网络序)][response protobuf]
```

### 7.3 ZooKeeper 路径

服务注册路径：`/${service_name}/${method_name}`，节点数据为 `host:port`。

### 7.4 测试

```bash
bash scripts/build.sh rpc
sudo systemctl start zookeeper
./bin/rpc_test_callee
./bin/rpc_test_caller
```

---

## 8. 构建与部署

### 8.1 环境依赖

**服务端（Linux）：**
```bash
sudo apt install -y build-essential cmake protobuf-compiler \
  libmysqlclient-dev libredis-dev libyaml-cpp-dev \
  libzookeeper-mt-dev zookeeper
```

**客户端（Linux）：**
```bash
sudo apt install -y build-essential cmake \
  libglfw3-dev libfreetype-dev libgl1-mesa-dev
```

**客户端（macOS）：**
```bash
xcode-select --install
brew install glfw freetype protobuf
```

### 8.2 构建

统一构建脚本 `scripts/build.sh`：

```bash
cd /path/to/sylar

# 构建服务端
bash scripts/build.sh server

# 构建客户端（需图形环境）
bash scripts/build.sh client

# 构建全部
bash scripts/build.sh all

# 构建 RPC 测试
bash scripts/build.sh rpc

# 清理
bash scripts/build.sh clean
```

### 8.3 数据库初始化

```bash
mysql -u root -p < ddt_server/schema.sql
```

需在 `ddt_server/conf/game.yml` 中配置数据库连接信息。

### 8.4 发布打包

```bash
bash scripts/build.sh release
# 输出: dist/ddt_client_linux.tar.gz, dist/ddt_client_macos.tar.gz
```

发布包内容：
- `src/` — 全部客户端源码（flat 结构）
- `thirdparty/` — glad、stb、imgui、freetype、protobuf_src、glfw、glm
- `assets/` — 字体 + 全部 PNG 纹理
- `shaders/` — GLSL 着色器
- `proto/` — Protobuf 定义
- `CMakeLists.txt` — 独立构建配置（standalone，无系统依赖）
- `ddt.sh` — 一键构建运行脚本

### 8.5 运行

```bash
./bin/ddt_server
./bin/ddt_client
```

---

## 9. 测试指南

### 9.1 本机测试

```bash
# 终端 1：启动服务端
bash scripts/build.sh server && ./bin/ddt_server

# 终端 2：启动客户端 1
bash scripts/build.sh client && ./bin/ddt_client

# 终端 3：启动客户端 2
./bin/ddt_client
```

客户端连接地址填 `127.0.0.1:8073`。

### 9.2 局域网两台电脑测试

```bash
# 服务端机器
cd /home/yinghuo/code/proj/sylar && bash scripts/build.sh server && ./bin/ddt_server

# 本机终端（端口转发）
ifconfig | grep 'inet ' | grep -v 127.0.0.1
brew install socat
socat TCP-LISTEN:8074,bind=0.0.0.0,fork TCP:192.168.139.145:8073

# 第二台电脑客户端连接
192.168.31.109:8074
```

### 9.3 公网连接 — cpolar

```bash
cpolar tcp 8073
# 客户端连接: 2.tcp.cpolar.cn:xxxxx
```

### 9.4 公网连接 — 樱花 FRP

1. 在 [Sakura FRP 官网](https://www.natfrp.com/) 创建 TCP 隧道（本地 IP：`127.0.0.1`，端口：`8073`）
2. 下载客户端并登录网页端控制台启动隧道
3. 通过控制台日志找到公网 IP 和端口

---

## 10. 实现细节

### 一、核心同步机制：权威服务器 + 表现层客户端

对于弹弹堂这类竞技游戏，防外挂和多端同步是重中之重。

**物理弹道同步方案：**
*   **服务端瞬间推演**：当玩家按下 FIRE 时，服务端 `GameRoom::handleShoot` 调用 `PhysicsEngine::computeTrajectory`。利用带有空气阻力、风力、重力的微积分公式（`AIR_FACTOR`, `WIND_FACTOR`），通过 `dt=0.01` 循环计算完整飞行轨迹点和落点。
*   **客户端插值播放**：服务端将带有时间戳的轨迹点数组通过 Protobuf 下发。客户端 `Projectile::Update` 不进行物理运算，仅根据 `m_elapsedTime` 在两个轨迹点间做线性插值。
*   **评价**：彻底杜绝客户端物理外挂，解决网络抖动导致的位置不同步。

### 二、客户端架构：模块化拆分

原 `game.cc`（1700+ 行 God Class）已拆分为 4 个独立模块：

| 模块 | 文件 | 职责 | 行数 |
|------|------|------|------|
| **GameRenderer** | `game_renderer.h/.cc` | 世界渲染（地形、人物、弹道、粒子、爆炸、伤害数字、小地图） | 205 |
| **GameNetwork** | `game_network.h/.cc` | 网络消息解析与游戏状态更新 | 437 |
| **GameHUD** | `game_hud.h/.cc` | ImGui 面板（HUD、房间列表、聊天、菜单） | 558 |
| **Game** | `game.cc` | 顶层协调（Init、ProcessInput、Update、updateCamera、Shutdown） | 630 |

各模块通过 `friend class` 访问 Game 的私有成员。

### 三、服务端架构：Actor 模式 + Session 管理

- **SessionManager**：封装 session→player 映射、账号踢人、心跳清理，解耦 Servlet 与 session 管理
- **Actor per Room**：`leaveRoom()` 通过 `post()` 提交到 Actor 队列，所有房间状态修改在 Actor 线程串行执行
- **Servlet handler 拆分**：200 行 switch-case → 独立 `handle*()` 方法，按消息类型分发

### 四、地形破坏：FBO + 延迟像素分配

- 使用 FBO（帧缓冲区对象）实现像素级地形破坏
- `RemoveCircle`：`glReadPixels` 读回 → Alpha 置 0 → `glTexSubImage2D` 传回
- **延迟分配**：`m_pixels`（16MB）不在 Init 预分配，首次 RemoveCircle 时从 GPU 读回
- **地形重建**：新增 `Terrain::Reset()` 方法，新一局开始时重新生成高度图+刷新 GPU 纹理

---

## 11. 版本更新日志（相对 v0.2）

### 架构重构

| # | 问题 | 修复 |
|---|------|------|
| R1 | Game God Class — game.cc 1700+ 行 | 拆分为 game.cc + game_hud + game_network + game_renderer 四模块 |
| R2 | 网络层 `void*` 裸指针 + `detach()` 不安全 | 改为 `shared_ptr<WSConnection>` + `join()` 安全回收 |
| R3 | Servlet 200行 switch-case 单点瓶颈 | 拆分为独立 `handle*()` 方法 |
| R4 | Servlet session 耦合 | 提取 `SessionManager` 类封装 session 生命周期 |
| R5 | Protobuf 单文件膨胀 | ddt.proto 通过 import 使用子 proto，删除冗余定义 |
| R6 | 物理参数客户端/服务端不同步 | `RoomReadyNotify` 下发 `physics_params` |
| R7 | 无回合状态校准机制 | `TurnStartNotify` 下发双方完整位置/HP/角度/方向 |

### Bug 修复

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 11 | 坑体中反抛射击失败，后续回合无法蓄力 | 服务端角度校验失败静默返回 + 射击锁未释放 + 弹道方向错误 | 服务端发送 ErrorNotify(460) + 客户端重置射击状态 + 本地弹道做方向转换 |
| 12 | 移动时人物贴图方向翻转 | MoveNotify 比较位置误判方向 | MoveNotify handler 仅更新对手方向，跳过本地玩家 |
| 13 | 面朝左但子弹向右飞 | 客户端本地弹道未做 `180-angle` 方向转换 | 根据 ShootResultNotify.direction 做方向转换 |
| 14 | 坑里射击被服务端拒绝 | 客户端用玩家中心 x 计算坡度，服务端用左边缘 x | 统一为 `Position.x`，每帧 clamp 角度 |
| 15 | 玩家贴图不随地形旋转 | `game_renderer.cc` rotation 硬编码 0.0f | 用 `getSlopeAngle` 计算坡度作为旋转角 |
| 16 | 对方退出后显示 "YOU LOSE!" | StatusText 不匹配游戏结束面板判断 | 改为 `"YOU WIN!"` |
| 17 | 对局结束退出房间无法连续开下一局 | 客户端回到大厅 + 服务端未重置 ready | 客户端回到 GAME_WAITING + 服务端重置 ready + 广播 |
| 18 | 新局状态混乱 + 幽灵房间 | 地形不重新生成 + removePlayer 线程不安全 + 定时器未取消 | Terrain::Reset() + leaveRoom 通过 Actor + cancelTurnTimer |
| 19a | 坑中角度不正确 | CurrentAngle 存绝对角度，坡度变化时范围突变 | 改为基准角度模式，滑块固定 [20,65]，发送时加 slopeAdj |
| 19b | 打中不掉血 | 先爆炸→重定位→再命中检测，距离超 hit_radius | 保存爆炸前位置，命中检测前移 |
| 19c | 自伤致死 | 命中检测不区分射手与目标 | 添加 isSelfOrAlly 判定，伤害上限 HP-1 |
| 19d | 蓄力交互错误 | ImGui Button click 模式 | 改用 IsItemActivated/Active/Deactivated hold-to-charge |

### 其他修复

| # | 问题 | 修复 |
|---|------|------|
| P0 | physics.cc Y 越界用 worldWidth | 改为 `cy > worldHeight` |
| P0 | Projectile 空轨迹未防御 | 空轨迹时 `m_active=false` 并提前返回 |
| P1 | 地形漂移（客户端/服务端挖坑算法不同） | 客户端 RemoveCircle 加 `+1.0f` |
| P1 | 移动限额硬编码 | RoomReadyNotify 下发 `max_move_per_turn` |
| P3 | Terrain 16MB 像素常驻内存 | 延迟分配，首次 RemoveCircle 时从 GPU 读回 |

### 改动文件总览

**客户端新增文件：**
- `ddt_client/logic/game_hud.h/.cc` — ImGui 面板模块
- `ddt_client/logic/game_network.h/.cc` — 网络消息处理模块
- `ddt_client/logic/game_renderer.h/.cc` — 世界渲染模块

**服务端新增文件：**
- `ddt_server/gate/ddt_session_mgr.h/.cc` — Session 管理器

**共享代码新增：**
- `common/ddt_physics.h/.cc` — 物理引擎（客户端/服务端共用）

**客户端修改：**
- `ddt_client/logic/game.h/.cc` — 从 1700+ 行重构为 630 行顶层协调者
- `ddt_client/logic/projectile.cc` — 空轨迹防御
- `ddt_client/main.cc` — 全局变量 ddt→g_game，Retina 适配
- `ddt_client/network/network_client.h/.cc` — shared_ptr + join + 自动重连
- `ddt_client/render/terrain.h/.cc` — Reset() 方法 + 延迟像素分配

**服务端修改：**
- `ddt_server/gate/ddt_main.cc` — FileLogAppender 日志落盘
- `ddt_server/gate/ddt_servlet.h/.cc` — handler 拆分 + SessionManager 集成
- `ddt_server/match/ddt_game_room.h/.cc` — 射击锁、回合校准、Actor 线程安全、命中检测修复
- `ddt_server/match/ddt_room_manager.h/.cc` — leaveRoom 通过 Actor、getRoomList 过滤空房间
- `ddt_server/battle/ddt_physics.h/.cc` — 修复 Y 越界，移至 common/ 共享
- `ddt_server/common/ddt_config.h/.cc` — 新增 max_move_per_turn 配置
- `ddt_server/db/ddt_database.cc` — 连接池改进

**协议修改：**
- `proto/ddt.proto` — import 子 proto，删除冗余定义
- `proto/room.proto` — 新增 max_move_per_turn 字段
- `proto/common.proto` — 共享类型定义
